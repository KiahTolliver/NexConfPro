@model IEnumerable<EventDto>
<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h1 class="text-white">Events List</h1>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-action="EventCreate" class="btn btn-outline-primary"><i class="bi bi-plus-square"></i> Create New Event</a>

            </div>
        </div>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Name
                    </th>
                    <th>
                        Date
                    </th>
                    <th>
                        Start Time
                    </th>
                    <th>
                        Location
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                    {
                <tr>
                <td>
                    @item.Name
                </td>
                <td>
                    @(item.Date.ToShortDateString())
                </td>
                <td>
                    @(item.StartTime.ToLocalTime())
                </td>
                <td>
                    @item.Location
                </td>
                <td>
                    <a class="btn btn-danger">
                        <i class="bi bi-trash"></i>
                    </a>
                </td>
                </tr>
                    }
            </tbody>
        </table>

    </div>

</div>

